// savingsGoal Component

// Asset Imports
import { LinearGradient } from "expo-linear-gradient";
import { Text, View, StyleSheet } from "react-native";

// Script Imports

// Types
type savingsGoalProps = {
    prop: type;
    prop: type;
};

// Returns a savingsGoal Component
export function savingsGoal({}: savingsGoalProps) {
    // Define Hook
    return (
    );
}

const styles = StyleSheet.create({
  class: {
    flex: 1,
  },
});

