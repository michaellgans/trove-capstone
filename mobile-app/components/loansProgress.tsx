// loansProgress Component

// Asset Imports
import { LinearGradient } from "expo-linear-gradient";
import { Text, View, StyleSheet } from "react-native";

// Script Imports

// Types
type loansProgressProps = {
    prop: type;
    prop: type;
};

// Returns a loansProgress Component
export function loansProgress({}: loansProgressProps) {
    // Define Hook
    return (
    );
}

const styles = StyleSheet.create({
  class: {
    flex: 1,
  },
});

