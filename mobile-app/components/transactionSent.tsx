// transactionSent Component

// Asset Imports
import { LinearGradient } from "expo-linear-gradient";
import { Text, View, StyleSheet } from "react-native";

// Script Imports

// Types
type transactionSentProps = {
    prop: type;
    prop: type;
};

// Returns a transactionSent Component
export function transactionSent({}: transactionSentProps) {
    // Define Hook
    return (
    );
}

const styles = StyleSheet.create({
  class: {
    flex: 1,
  },
});

